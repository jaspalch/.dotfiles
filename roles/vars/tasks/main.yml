- name: "Get actual hostname"
<<<<<<< HEAD
  command: "uname -n"
  register: "hostname_cmd"
- set_fact:
    host_type: "{{ 'work' if hostname_cmd.stdout in work_hosts else 'personal' }}" 

- name: "Get OS type"
  command: "uname -o"
  register: "os_cmd"
- set_fact:
    os_type: "{{ os_cmd.stdout }}"
=======
  command: uname -n
  register: hostname_cmd
- set_fact:
    host_type: "{{ 'work' if hostname_cmd.stdout in work_hosts else 'personal' }}" 
>>>>>>> f85845e... Fix work/personal hostname matching
