# ~/ User home dir
user_home: "{{ '~' | expanduser }}"
# ~/dotfiles/ dotfiles dir location
dotfiles_home: "{{ user_home }}/.dotfiles"

# Specify work/personal hosts
work_hosts: 
  - "Jaspals-Macbook-Pro"
  - "jaspal-desktop"
personal_hosts: 
  - "photon"
# Create var to define host type
host_type: "{{ 'work' if inventory_hostname in work_hosts else 'personal' }}"
